

export const EVENT_TYPES = Object.freeze({
	// Generic
	TEST: Symbol('SET_CURRENT_HOVER_ROW_INDEX')
});


const reducer = (state, action) => {
	switch (action.type) {
		default: {
			return state;
		}
	}
};

export default reducer;
